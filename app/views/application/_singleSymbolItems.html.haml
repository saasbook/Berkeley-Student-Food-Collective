- if items.length > 0
  - items.each do |item|
    %tr{class: "#{myclass.name.underscore}s_row"}
      %td{class: "#{myclass.name.downcase}s"}
        - path = {controller: "#{myclass.name.underscore}s", action: "show", id: item.id}
        = button_to "#{item.name}", path, {method: :get, class: 'prod-link'}
- else
  %tr{id: "#{sym}"}

