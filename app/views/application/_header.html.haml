%header
  .navbar.navbar-expand-lg.navbar-dark.static-top
    .container-fluid
      .navbar-header
        = link_to discovery_path, id: 'logo', class: 'navbar-brand w-50' do
          = image_tag 'logo450x429.png', id: 'logoheader'
      %button.navbar-toggler{type: 'button', data: {toggle: 'collapse', target: '#navbarResponsive'}, aria: {expanded: 'false', controls: 'navbarResponsive', label: 'Toggle navigation'}}
        %span.navbar-toggler-icon
      #navbarResponsive.collapse.navbar-collapse.w-100
        %ul.navbar-nav.w-100
          -if admin
            %li.navbar-text ADMIN
            %li.nav-item.admin{class: ('active' if currentPage == 'vendor')}
              = link_to 'Vendors', admin_vendors_path, class: 'nav-link'
            %li.nav-item{class: ('active' if currentPage == 'product')}
              = link_to 'Products', admin_products_path, class: 'nav-link'
            %li.nav-item{class: ('active' if currentPage == 'tag')}
              = link_to 'Tags', admin_tags_path, class: 'nav-link'
          
          -else
            %li.nav-item{class: ('active' if currentPage == 'discovery')}
              = link_to 'Discovery', discovery_path, class: 'nav-link'
            %li.nav-item{class: ('active' if currentPage == 'vendor')}
              = link_to 'Vendors', vendors_path, class: 'nav-link'
            %li.nav-item{class: ('active' if currentPage == 'product')}
              = link_to 'Products', products_path, class: 'nav-link'
            %li.nav-item{class: ('active' if currentPage == 'product_tags')}
              = link_to 'Product Tags', product_tags_path, class: 'nav-link'
            %li.nav-item{class: ('active' if currentPage == 'producer_tags')}
              = link_to 'Producer Tags', producer_tags_path, class: 'nav-link'
        %button 
          =button_to 'Login', discovery_path, {method: :get, class: 'prod-link'}
      -if admin
        .dropdown
          %button.dropbtn
            %i.fa.fa-plus
          .dropdown-content
            = button_to 'New Product', new_admin_product_path, {method: :get, class: 'prod-link'}
            = button_to 'New Vendor', new_admin_vendor_path, {method: :get, class: 'prod-link'}
            = button_to 'New Product Tag', new_product_tag_path, {method: :get, class: 'prod-link'}
            = button_to 'New Producer Tag', new_producer_tag_path, {method: :get, class: 'prod-link'}
            = button_to 'New Tag Category', new_item_tag_category_path, {method: :get, class: 'prod-link'}
