%div{:class => "container carousel_element #{local_assigns[:ownership]}", :id => local_assigns[:vendor].name}
	%button
		= image_tag "test.jpg", :class => "image"
	%div{:class => "bottom-left"} 
		%td= local_assigns[:vendor].name