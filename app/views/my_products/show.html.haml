= render 'header', currentPage: 'my_product'
#details
  #detailsPageHeader
    %h1.item_name
      = @product.name
      -if current_admin
        .edit_button
          %a{href: edit_my_product_path(@product)} Edit
          
  = render 'all_content'